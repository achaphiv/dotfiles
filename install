#!/usr/bin/env bash

set -ex

which ansible-playbook || sudo apt-get install -y ansible
# FIXME: ansible 2.0.0.2 doesn't ask for password correctly (issue #13728)
sudo echo
ansible-playbook -vv site.yml
