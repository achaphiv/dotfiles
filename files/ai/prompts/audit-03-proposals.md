Use openspec-propose skill to create change(s) on separate branches.

Split large changes into multiple small changes where possible.
This eases the review process.

Append a number to each branch like `openspec/${CHANGE_NAME}-01` to indicate the intended ordering.

After the branches are created, use the `x-review` skill separately per branch.

Rejected specs/branches should just be removed.
