Audit `git ls-files backend-adapter-jooq/src/main/java/**.java` to find code quality issues.

E.G. excessive duplication, bad architecture, long methods, unnecessary complexity, etc.

Write findings to a `FINDINGS-{yyyy-mm-dd}.md`.

Use the `x-review` skill on the generated FINDINGS.
