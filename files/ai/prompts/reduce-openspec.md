Reduce token usage of openspec/specs/

The underlying intent/idea should be kept. Just express that same information in a less verbose way.

`openspec validate --strict --all` should still pass after changes.

Use 2 adversarial subagents to review the changes.

Make commits. No need for `mise run test` for doc only changes.
