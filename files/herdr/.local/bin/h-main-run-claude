#!/usr/bin/env bash

set -euo pipefail

if (( $# == 0 )); then
  printf 'usage: h-main-run-claude PROMPT...\n' >&2
  exit 64
fi

exec h-main-run claude "$*" --dangerously-skip-permissions --system-prompt ''
