---
model: opus
effort: high
disable-model-invocation: true
description: 'fix'
---

Create a branch `fix/{CHANGE_NAME}`.

Fix what has been requested. Make commits.

Once done, use `x-review` skill.

Finallly, merge this branch back into `main` with `--no-ff` and delete this branch.



